[package]
name = "mkxp-config"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tracing = "0.1"
mkxp-types = { path = "../mkxp-types" }
config = { version = "0.15", features = ["ron", "ini"] }
serde = { version = "1", features = ["derive"] }
merge = "0.1"
clap = { version = "4", features = ["derive"] }
thiserror = "2"
