/// 纹理缓存池。
///
/// 对应 mkxp-z 的 `TexPool`。
/// 当前为空骨架。
pub struct TexPool;

impl Default for TexPool {
    fn default() -> Self {
        Self::new()
    }
}

impl TexPool {
    pub fn new() -> Self {
        Self
    }
}
